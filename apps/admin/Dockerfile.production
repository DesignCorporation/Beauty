# BEAUTY PLATFORM - ADMIN PANEL PRODUCTION DOCKERFILE
# Optimized: Vite build done on CI, only copy dist to Nginx

FROM nginx:alpine

# Copy pre-built static assets from CI
COPY dist /usr/share/nginx/html

# Custom Nginx config to handle SPA routing (try_files) and port 6002
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 6002

CMD ["nginx", "-g", "daemon off;"]
