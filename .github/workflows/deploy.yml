name: Deploy (manual)

on:
  workflow_dispatch:
    inputs:
      target_branch:
        description: "Branch to deploy"
        required: true
        default: "main"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch }}

      - name: Deploy placeholder
        run: |
          echo "TODO: add rsync/ssh deploy steps to /opt/beauty-prod"
          echo "Requires secrets: SSH_HOST, SSH_USER, SSH_KEY, SSH_PATH"
